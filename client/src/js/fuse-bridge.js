export const sendUserAccountInfoToFuse = (accessToken, email, name)=>{
  console.log("sendUserAccountInfoToFuse", accessToken, email, name);
};

export const closeWebModal = ()=>{
  console.log("closeWebModal");
};
